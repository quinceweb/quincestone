{
  "name": "@quincestone/types",
  "private": true,
  "version": "1.0.0",
  "exports": { "./intelligence": "./src/intelligence.ts", "./tenant": "./src/tenant.ts" },
  "scripts": { "lint": "tsc --noEmit", "typecheck": "tsc --noEmit", "test": "vitest run", "build": "tsc --noEmit", "check": "pnpm lint && pnpm typecheck && pnpm test && pnpm build" },
  "dependencies": { "zod": "^3.25.0" },
  "devDependencies": { "typescript": "^5.5.0", "vitest": "^3.0.0" }
}
