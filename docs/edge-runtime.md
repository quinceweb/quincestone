# Edge runtime

The current build models the edge layer in the browser for demonstration. A future edge API may accept consented events, retrieve approved knowledge, apply deterministic policy, invoke bounded AI, and return an explainable decision envelope.

The `VITE_EDGE_API_URL` variable is reserved; no production edge API is claimed.
