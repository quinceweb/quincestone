# AI governance

AI may clarify or summarize supplied context. It must not silently make consequential decisions. Deterministic policy owns service-area checks, workflow selection, escalation, and safety boundaries. Human review is required when policy indicates material risk or uncertainty.

Prompt, model, knowledge version, decision reason, and override should be observable in a future protected runtime.
