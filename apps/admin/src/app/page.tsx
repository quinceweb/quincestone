import Link from "next/link";
import { requirePlatformRole } from "@/lib/platform-authority";
export const dynamic = "force-dynamic";
export default async function AdminHome() { let authorized = false; try { await requirePlatformRole("operator"); authorized = true; } catch {} return <div className="shell"><aside className="sidebar"><div><div className="brand">QUINCESTONE</div><div className="brand-subtitle">Commerce control plane</div></div><nav className="nav" aria-label="Control plane navigation"><p className="nav-label">Platform</p><Link href="/">Overview</Link><Link href="/commerce">Commerce</Link><Link href="/commerce/media">Media review</Link><a href="#organizations">Organizations</a><a href="#access">Users & Access</a><a href="#operations">Operations</a><a href="#audit">Audit</a></nav><div className="sidebar-foot"><span>Internal surface</span><span>{authorized ? "Operator authority" : "Authority required"}</span></div></aside><main className="main"><div className="content"><div className="header"><div><div className="eyebrow">QUINCESTONE / CONTROL PLANE</div><h1>Govern the company. Operate the commerce.</h1><p className="lede">Platform administration is separate from customer workspace administration. Commerce operations are server-authorized and fail closed when authority is absent.</p></div><div className="status"><div className="status-label">Authority</div><div className="status-value">{authorized ? "Authorized" : "Not configured"}</div></div></div><div className="notice"><strong>Production boundary</strong><span>Customer workspace roles do not grant platform or commerce operations authority. Supplier purchases remain human-approved.</span></div><div className="grid"><section className="section"><h2>Commerce</h2><p>Products, launch gates, pricing, orders, fulfillment, returns and supplier operations.</p><Link className="text-link" href="/commerce">Open commerce operations →</Link></section><section className="section"><h2>Media review</h2><p>Approve, reject, classify, verify, restrict and unpublish media without bypassing product launch gates.</p><Link className="text-link" href="/commerce/media">Open media review →</Link></section><section className="section" id="organizations"><h2>Organizations</h2><p>Platform-level tenant lifecycle and service state, separate from workspace permissions.</p></section><section className="section" id="access"><h2>Users & access</h2><p>Independent administrator and operator authority with server-side verification.</p></section><section className="section" id="operations"><h2>Operations</h2><p>Runtime, provider and intervention state without exposing customer secrets.</p></section><section className="section" id="audit"><h2>Audit</h2><p>Durable records for privileged actions, product decisions and governance boundaries.</p></section></div></div></main></div>; }
