export default function InboxPage(){return <main className="shell workspace-page"><header className="workspace-header"><div><p className="section-kicker">Decision inbox</p><h1>What requires you.</h1><p>Not notifications. Commercial work that needs a decision, response or follow-through.</p></div></header><div className="inbox-sections">{["Needs my action","Waiting","Changed","Approvals","Expiring"].map((title)=><section key={title}><div><h2>{title}</h2><span>0 verified items</span></div><p>No persistent deal source is connected, so Quincestone cannot assert that work is waiting here.</p></section>)}</div></main>}
